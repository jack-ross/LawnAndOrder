{
  "name": "navigation-server",
  "version": "1.0.0",
  "description": "Navigation server to send messages to robots based on CV coordinates",
  "main": "navigation-server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-mqtt": "/usr/local/sbin/mosquitto -c ../mosquitto.conf",
    "start": "node ./navigation-server/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^2.1.3"
  }
}
